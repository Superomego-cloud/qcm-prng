"""
Implémentation de PRNG
Elyas Sahnoune
"""

import math

graine: int

def generer_nombre(a: int, b: int) -> int:

    """
    Fonction qui génère un nombre aléatoire compris entre a et b 
    """

def generer_permutation(entree:list) -> list:
    """
    Fonction qui prend une liste mutable d'éléments et qui retourne une liste
    contenant les mêmes éléments que le paramètres mais pas nécéssairement dans
    le même ordre
    """

def tester_prng(constr: type, nbr_iter: int = 100_000, nbr_graines: int = 10) -> bool:
    """
    Fonction qui prend un constructeur de PRNG, un nombre d'itérations, et un nombre de graines à tester, et retourne une booléene qui dit si l'implémentation est correcte ou non
    """


