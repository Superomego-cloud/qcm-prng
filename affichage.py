from interne import *

# quand tu as une expression de la forme var: type, c'est à dire que le type de var est celui indiqué (par exemple x: int revient à dire x est un int)
def afficher_questionnaire(q : list[int]):
    """
    Il s'agit de la fonction principale qui affiche le code
    """
